node_modules/
.DS_Store
*.log
.claude/

# Electron build output (future packaging)
dist/
build/
out/

# Defensive — these live in Electron's userData dir, but in case a test
# leaks them into the project tree, don't commit user-specific state.
permissions.json
defaults.json
