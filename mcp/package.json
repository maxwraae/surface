{
  "name": "surface-mcp",
  "version": "0.3.0",
  "description": "MCP server for Surface — one tool, `surface`, that shows the user files, URLs, or HTML the agent just wrote.",
  "type": "module",
  "main": "index.js",
  "bin": {
    "surface-mcp": "./index.js"
  },
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
